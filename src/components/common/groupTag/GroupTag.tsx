import * as React from 'react';
import './groupTag.css';

export function GroupTag({group}: {group: string}) {
    return (
        <div className='groupTag'>
            contemporary
        </div>
    );
}